import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.menumeroo.app',
  appName: 'menumeroo',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
